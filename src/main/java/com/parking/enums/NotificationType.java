package com.parking.enums;

/**
 * Persisted notification event types published through the notification backbone.
 */
public enum NotificationType {
    ENTRY_EXIT,
    PAYMENT_SUCCESS,
    MAINTENANCE,
    WEEKLY_SUMMARY;
}
